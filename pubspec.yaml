name: orb_test_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

workspace:
  - local_packages/orb_ui_kit

dependencies:
  bloc_concurrency: ^0.3.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  helm: ^0.0.1
  http: ^1.2.2
  intl: ^0.20.2
  meta: ^1.16.0
  orb_ui_kit: ^0.0.1
  rxdart: ^0.28.0
  shared_preferences: ^2.3.3

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  generate: true
  uses-material-design: true
